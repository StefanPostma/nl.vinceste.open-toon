'use strict';

const WebAPIError = require('./WebAPIError');

class WebAPIClientError extends WebAPIError {
	constructor(message, options) {
		super(message, options);
		this.name = 'WebAPIClientError';
	}
}

module.exports = WebAPIClientError;
