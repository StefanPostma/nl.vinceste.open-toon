'use strict';

const WebAPIError = require('./WebAPIError');

class WebAPIServerError extends WebAPIError {
	constructor(message, options) {
		super(message, options);
		this.name = 'WebAPIServerError';
	}
}

module.exports = WebAPIServerError;
